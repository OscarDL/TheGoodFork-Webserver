{
  "name": "thegoodfork-heroku",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm i",
    "start": "cd server && node server.js",
    "heroku-postbuild": "cd client && npm i && npm run build"
  }
}